@model IEnumerable<Web.Models.Course>

@{
    ViewBag.Title = "Index_Teacher";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create_Course")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Teacher.TName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Teacher.TName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit_Course", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details_Course", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete_Course", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }

</table>
